# Cineverse Hub ML Backend - Environment Configuration

# Server configuration
HOST=0.0.0.0
PORT=8000
RELOAD=false

# Allowed CORS origins (comma-separated or * for all)
CORS_ORIGINS=https://movierecobox.vercel.app,http://localhost:5173,http://localhost:3000

# Optional custom path for model bundle (defaults to clustering_model.pkl in root)
# MODEL_PATH=clustering_model.pkl
